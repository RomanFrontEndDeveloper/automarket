import Navigation from '../Navigation/Navigation';

const Header = () => {
	return (
		<header className='header'>
			<h2>🚗 AutoMarket</h2>

			<Navigation />
		</header>
	);
};

export default Header;
